X = 9999
import string #  when import from cwd spam,either in python 3 or 2 there is no way
              # to load the python std lib string because the search goes
              # thru cwd first and then the python path
              # If string was define in pkg python 3 would have skipped it.